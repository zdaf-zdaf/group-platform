name: Lab Service CI/CD

on:
  push:
    paths:
      - 'backend/lab-service/**'  # 只监控lab-service目录的变更
    branches: [ "master" ]
  pull_request:
    paths:
      - 'backend/lab-service/**'
    branches: [ "master" ]

jobs:
  test-lab-service:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: rootpassword
          MYSQL_DATABASE: lab_db
          MYSQL_DEFAULT_AUTHENTICATION_PLUGIN: mysql_native_password
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt
        working-directory: ./lab-service

      - name: Wait for MySQL
        run: |
          sudo apt-get update && sudo apt-get install -y mysql-client
          for i in {1..30}; do
            if mysqladmin ping -h"127.0.0.1" -uroot -prootpassword --silent; then
              echo "MySQL is ready!"; break
            fi
            echo "Waiting for MySQL... (attempt $i/30)"; sleep 2
          done

      - name: Run migrations
        run: |
          python manage.py makemigrations --noinput
          python manage.py migrate --noinput
        working-directory: ./lab-service
        env:
          DB_ENGINE: django.db.backends.mysql
          DB_NAME: lab_db
          DB_USER: root
          DB_PASSWORD: rootpassword
          DB_HOST: 127.0.0.1
          DB_PORT: 3306

      - name: Run unit tests
        run: python manage.py test --verbosity=2
        working-directory: ./lab-service
        env:
          DB_ENGINE: django.db.backends.mysql
          DB_NAME: lab_db
          DB_USER: root
          DB_PASSWORD: rootpassword
          DB_HOST: 127.0.0.1
          DB_PORT: 3306

  build-and-push-lab-service:
    runs-on: ubuntu-latest
    needs: test-lab-service
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Build and push lab service image
        uses: docker/build-push-action@v5
        with:
          context: ./lab-service
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/lab-service:latest
            ghcr.io/${{ github.repository_owner }}/lab-service:${{ github.sha }}

  deploy-lab-service:
    runs-on: ubuntu-latest
    needs: build-and-push-lab-service
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Generate deployment script for lab service
        run: |
          cat > deploy-lab-service.sh << 'EOF'
          #!/bin/bash
          echo "Deploying Lab Service..."
          
          # Update image version
          sed -i "s|image:.*lab-service:.*|image: ghcr.io/${{ github.repository }}/lab-service:${{ github.sha }}|g" lab-service/k8s/deployment.yaml
          
          # Apply Kubernetes configuration
          kubectl apply -f lab-service/k8s/
          
          echo "Lab Service deployed!"
          EOF
          
          chmod +x deploy-lab-service.sh

      - name: Upload deployment script
        uses: actions/upload-artifact@v4
        with:
          name: lab-service-deployment
          path: deploy-lab-service.sh